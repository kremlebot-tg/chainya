# Checkout backend

Сервер сам считает цены по постоянному каталогу, сохраняет заказы в SQLite и
подключает Т-Банк, CDEK, Saby и Telegram. Тестовая оплата
регистрируется только при явном `CHAINYA_TEST_MODE=1`; в production её маршрутов
нет.

```bash
cd /Users/mac/Desktop/Чайня/site
python3 -m venv .venv
.venv/bin/pip install -r backend/requirements-dev.lock.txt
python3 build.py --web
CHAINYA_TEST_MODE=1 .venv/bin/uvicorn backend.app:app --reload --port 8080
```

Открыть `http://127.0.0.1:8080`. База создаётся в `backend/data/` и не попадает
в Git.

## API

- `GET /api/health` — состояние и число позиций каталога.
- `POST /api/delivery/quote` — актуальный расчёт доставки СДЭК по корзине и городу.
- `POST /api/orders` — серверная проверка, расчёт и сохранение заказа.
- `GET /api/orders/{id}` — состояние заказа.
- `POST /api/account/register`, `POST /api/account/login`, `GET/DELETE /api/account/session` — регистрация, вход, тихая проверка сессии и выход клиента.
- `GET/PATCH/DELETE /api/account` — профиль клиента, изменение имени и удаление кабинета.
- `GET /api/account/orders`, `POST /api/account/orders/claim` — история и защищённая привязка гостевого заказа.
- `GET /api/account/bookings`, `POST /api/account/bookings/{id}/cancel` — бронирования клиента и безопасная отмена своей будущей брони.
- `POST /api/orders/{id}/test-pay` — имитация webhook успешной оплаты, только
  при `CHAINYA_TEST_MODE=1`.
- `POST /api/analytics/events` — обезличенное событие сайта без IP, cookies и user-agent.
- `GET /api/bookings/availability?date=YYYY-MM-DD` — свободные получасовые старты на выбранный день.
- `POST /api/bookings` — атомарно сохраняет двухчасовую бронь; при пересечении возвращает `409`.
- `POST /api/bookings/{id}/cancel` — идемпотентная отмена по приватному токену из подтверждения сайта или Telegram; освобождает окно без удаления записи.
- `POST /api/promos/preview` — серверная проверка промокода и расчёт скидки по актуальному каталогу.
- `GET /api/admin/dashboard?days=30` — агрегаты, графики, воронка и состояние систем.
- `GET /api/admin/catalog` — полный каталог для панели владельца.
- `GET/POST/PUT /api/admin/promos` — управление процентными промокодами, сроком действия, минимальной суммой и включением. Скидка распределяется через товарные цены, поэтому сумма строк совпадает с оплатой, чеком и Saby; доставка не дисконтируется.
- `GET /api/admin/catalog/history?limit=50` — безопасный журнал изменений
  каталога без снимков карточек, ключей и других чувствительных данных.
- `POST /api/admin/catalog/items`, `PUT /api/admin/catalog/items/{id}` — добавление и изменение товара.
- `PUT /api/admin/catalog/order` — порядок товаров на витрине.
- `POST /api/admin/catalog/items/{id}/image?revision=N` — замена галереи одной главной фотографией.
- `POST /api/admin/catalog/items/{id}/images?revision=N` — проверка и добавление фото в галерею (до восьми).
- `PUT/DELETE /api/admin/catalog/items/{id}/images/{index}` — выбор главного и удаление фото из карточки без удаления backup-файла.
- `GET /api/admin/orders?q=&status=&limit=50&offset=0` — поиск и постраничная очередь заказов.
- `GET /api/admin/orders/{id}/tbank/status` — read-only повторная проверка
  фактического статуса оплаты в Т-Банке без изменения заказа.
- `POST /api/admin/orders/{id}/tbank/reconcile` — явное восстановление
  локального состояния после потерянного webhook. Метод выполняет только
  `GetState`, требует совпадения суммы и доступных идентификаторов, не создаёт
  платёж/списание/возврат и применяет ту же таблицу переходов, что подписанное
  уведомление банка.
- `POST /api/admin/orders/{id}/saby/receipts/{kind}/check` — ручная read-only
  сверка уже известного `payId` через `/retail/pay/list`; не создаёт продажу и
  недоступна без сохранённого идентификатора чека.
- `GET /api/admin/business-leads?q=&status=&limit=50&offset=0` — поиск и постраничные B2B-заявки.
- `GET/POST /api/admin/booking-blocks`, `DELETE /api/admin/booking-blocks/{id}` — ручное закрытие и открытие времени владельцем.
- `GET /api/admin/saby/status` — готовность конфигурации Saby без раскрытия секретов.
- `POST /api/admin/saby/test` — явная read-only проверка Saby Retail:
  выбранной точки, прайс-листа, каталога, остатков и соответствий. При
  настроенном фискальном маршруте она без раскрытия идентификаторов проверяет,
  видит ли сервисное приложение компанию. Состояние физической ККТ этот
  Retail-запрос не подтверждает: список касс требует отдельную OFD-сессию.
  Проверка не выполняет пробную запись и не регистрирует чек.
  Запрос требует
  заголовок намерения админ-панели, имеет лимит 6 запусков за 5 минут,
  выполняется в одном экземпляре и возвращается с `Cache-Control: no-store`.
- `GET /api/admin/saby/catalog-preview` — до 50 позиций выбранного прайс-листа.
- `GET /api/admin/saby/catalog-review` — read-only предложения для связывания
  новых позиций с безопасным неопубликованным черновиком сайта.
- `GET /api/admin/saby/catalog-shadow?history_limit=10` — последний read-only
  отчёт сравнения сайта с Saby и ограниченный журнал запусков.
- `POST /api/admin/saby/catalog-shadow/run` — ручная теневая сверка без изменения
  сайта, кассы, цен, остатков или заказов.
- `GET /api/admin/integrations/status` — сетево-независимая готовность Т-Банка, Saby и CDEK.
- `GET /api/admin/orders/{id}/integration-preview?ready_at=YYYY-MM-DD HH:MM:SS` —
  предварительные входные данные интеграций без внешних запросов и записей.

В production стоимость и доступность доставки рассчитываются через CDEK API,
а платежи создаются через Т‑Банк. Mock-оплата существует только при явном
`CHAINYA_TEST_MODE=1` и недоступна на production-vhost. Секреты передаются
только через защищённые переменные окружения.

Подготовительные адаптеры находятся в `tbank.py`, `cdek.py` и `saby_sync.py`.
Они ничего не запускают при импорте. Все будущие внешние записи обязаны идти
через `IntegrationWriter`: единый guard запрещает их в `CHAINYA_TEST_MODE`, при
неопубликованном workflow и в режимах `off`/`shadow`. Только точное значение
`CHAINYA_TEST_MODE=0` отключает тестовый предохранитель; опечатки остаются в
безопасном тестовом режиме.

Панель владельца открывается по `/manage`. Ключ вводится на отдельной странице
входа и проверяется сервером. После успешной проверки сервер выдаёт
`HttpOnly; Secure; SameSite=Strict` cookie на 12 часов; сырой ключ не хранится
в JavaScript и браузерном хранилище. Старые ссылки `/manage#ADMIN_TOKEN` и
`/admin/orders#ADMIN_TOKEN` остаются переходным способом входа: минимальная
страница отправляет ключ серверу, сразу удаляет fragment из адреса и получает
ту же защищённую сессию. Без сессии полноценная HTML-оболочка панели не
выдаётся. Выход выполняется кнопкой в правом верхнем углу.

### Управление каталогом

Раздел `/manage/catalog` использует ту же защищённую сессию. Новый товар сначала
создаётся как чай или как посуда. Для чая редактор показывает цену за 10 г,
маркировку и вкусовой профиль; для посуды — поштучную цену, материал и технику,
мастера или изготовителя, размеры и рекомендации по уходу. В нём можно менять
название, подзаголовок и описание на русском, английском и китайском, цену,
единицу продажи, наличие, публикацию, категорию, галерею до восьми фото и
порядок товаров и фотографий. Перед сохранением доступен предпросмотр, а после
загрузки можно выбрать главное фото. Владелец также может создать безопасный черновик на основе
существующей карточки, отфильтровать незаполненные позиции, выгрузить текущий
каталог в JSON и просмотреть краткий журнал изменений. Для каждой локали также
предусмотрены поля маркировки:
состав, изготовитель/упаковщик/импортёр, срок годности и условия хранения.
Пустые поля не показываются покупателю и не блокируют публикацию. Панель считает
их отдельно по языкам, показывает предупреждение и позволяет отфильтровать
карточки, которые нужно дополнить; на незаполненном языке витрина использует
доступный перевод. Сведения о пищевом товаре всё равно нужно дополнить по
фактической этикетке, не по предположению. Удаление намеренно заменено безопасным переключателем
«Показывать на сайте»: скрытая позиция исчезает с витрины и не принимается в
новых заказах, но остаётся в истории.

Категория выбирается прямо в карточке товара. Кнопка «Категории» позволяет без
разработчика создавать и переименовывать разделы для чая и посуды, задавать
названия на русском, английском и китайском и менять их порядок. Базовые
категории защищены от удаления, а занятую категорию нельзя удалить или
перевести между группами, пока товары не распределены. Для чая изначально
доступны в том числе «Жёлтый чай» и «Тайваньские улуны». На витрине и в
Telegram пустая категория не показывается: фильтр появляется автоматически
после публикации первого товара этой категории.

Кнопка «Открыть сверку СБИС» загружает данные кассы только для просмотра и
показывает отдельно количество позиций в выбранном прайс-листе сайта и в
основном каталоге СБИС. Это разные области: услуги, временно снятые позиции и
товары других прайс-листов могут присутствовать только в основном каталоге.
Панель явно показывает такие позиции как находящиеся вне прайс-листа сайта, но
не позволяет создать из них черновик, пока владелец не добавит нужный товар в
прайс-лист внутри СБИС. Новая позиция выбранного прайс-листа
открывается в редакторе как скрытый черновик: название, подтверждённая цена и
наличие подставляются из СБИС, а категорию, фотографию, описание и маркировку
владелец проверяет самостоятельно. Черновик, созданный из СБИС, нельзя
опубликовать с временной фотографией. После загрузки настоящего фото связь с
номенклатурой сохраняется и используется при передаче оплаченного заказа.
Ни просмотр, ни создание черновика не меняют карточку или прайс-лист в СБИС.

### Управление содержанием сайта

Раздел `/manage/site` использует ту же защищённую сессию и предназначен для
редактируемых блоков публичных страниц. Первый модуль — партнёры на странице
«Для бизнеса». Владелец может добавить скрытый черновик, изменить название и
короткую подпись на русском, английском и китайском, опубликовать запись,
поменять порядок и посмотреть историю изменений. Для публикации достаточно
названия хотя бы на одном языке; незаполненные переводы и подписи остаются
видимыми предупреждениями и используют безопасный fallback на витрине.
Безвозвратного удаления в интерфейсе нет: запись скрывается с сайта, но остаётся
в хранилище и резервных копиях. Партнёры лежат в том же атомарном
`catalog.json`, поэтому защищены общей ревизией от одновременной перезаписи и
автоматически входят в существующий backup-контур.

Раздел `/manage/guides` использует ту же защищённую сессию и содержит
полнотекстовый поиск и тематические фильтры по рабочим инструкциям: начало дня,
заказы, брони, B2B-заявки, промокоды, каталог, сайт, Saby, Т-Банк, CDEK,
резервные копии и частые ошибки. Каждый рабочий сценарий отделяет безопасное
чтение от изменений сайта и реальных внешних операций, заканчивается критерием
готовности и при необходимости ведёт прямо в нужную вкладку панели. Статическая
инструкция не выдаёт меняющееся состояние production за факт: фактическая
готовность интеграций всегда читается из живых индикаторов «Обзора». В гайдах
нет кнопок внешних записей: денежные операции и создание отправлений только
объясняются вместе с их безопасными границами.

Production-каталог хранится в `/var/lib/chainya-shop/catalog.json`, фотографии —
в `/var/lib/chainya-shop/catalog-media/`. Оба ресурса переживают новые релизы и
попадают в ежедневную резервную копию. При первом запуске каталог атомарно
создаётся из `backend/catalog.seed.json`; последующие релизы пользовательские
изменения не перезаписывают. Витрина получает опубликованный каталог из
`GET /api/catalog`, а встроенные данные `src.html` остаются fallback на случай
недоступности API.

Каждый опубликованный товар также получает три индексируемые страницы. Для чая
это `/tea/<id>`, `/en/tea/<id>` и `/zh/tea/<id>`, а для посуды — соответствующие
маршруты `/teaware/<id>`. Они содержат локализованные
title, description, canonical, взаимные `hreflang`, Open Graph и Product
JSON-LD. Страницы и динамический `/sitemap.xml` читают тот же живой каталог:
изменение карточки или скрытие товара в панели не требует нового релиза сайта
и автоматически отражается в поисковом контуре.

Главная, каталоги чая и посуды, бизнес-раздел и бронирование также имеют
отдельные RU/EN/中文 URL: `/`, `/en/`, `/zh/` и соответствующие локализованные
маршруты разделов. Каждый вариант содержит собственные title, description,
canonical и взаимные `hreflang`; динамический sitemap объединяет эти страницы
с актуальными опубликованными товарами.

Исходный seed пересобирается после намеренного изменения встроенного fallback:

```bash
python3 scripts/build-catalog-seed.py
```

Редактор не меняет кассовый прайс-лист и не публикует предложения Saby без
явного решения владельца.

### Личный кабинет клиента

Кабинет открыт по `/account`. Клиент регистрируется по номеру
телефона и паролю длиной не менее 10 символов. Пароль хранится только как
PBKDF2-SHA256-хеш с отдельной случайной солью. После входа сервер выдаёт
случайную сессию в `HttpOnly; SameSite=Strict` cookie: на production она также
имеет флаг `Secure`, действует 30 дней, а в SQLite хранится только хеш токена.

Заказ или бронь с сайта автоматически связываются с кабинетом, только если они
созданы во время действующей клиентской сессии. Совпадение телефонных номеров
само по себе никогда не открывает старые записи. Гостевой заказ можно присоединить через
кнопку на защищённой странице результата оплаты: сервер требует одновременно
номер заказа, его непубличный token и клиентскую сессию. Уже привязанный к
другому кабинету заказ перенести через публичный API нельзя.

В кабинете доступны до 100 последних заказов и броней, состав, сумма, статусы и
ссылка на ещё не завершённую оплату. Клиент может изменить отображаемое имя и
пароль, выйти или удалить кабинет. Смена пароля завершает остальные сессии.
Удаление кабинета удаляет профиль и сессии и отвязывает историю, но не удаляет
саму запись заказа: сроки хранения заказа определяются требованиями учёта и
политикой конфиденциальности. Восстановление забытого пароля до подключения
проверенного SMS/email-канала выполняется через поддержку; небезопасного
сброса только по номеру телефона нет.

Отдельное согласие на обработку персональных данных опубликовано по
`/consent-personal-data.html`. Для каждого нового заказа, брони, обращения для
бизнеса и кабинета сервер атомарно создаёт запись в
`personal_data_consents`: тип и идентификатор исходной записи, назначение
формы, серверную версию текста, время и канал отправки. IP-адрес и цифровой
отпечаток для доказательства согласия не добавляются. Старые записи не
помечаются новым согласием задним числом.

### Единое расписание броней

Сайт и Telegram-бот используют таблицу `bookings` в одной SQLite-базе. Новая
или подтверждённая бронь занимает единственный стол на 120 минут. Сервер
проверяет пересечение внутри `BEGIN IMMEDIATE`, поэтому параллельные запросы не
могут создать двойную бронь. Статусы `cancelled` и `completed` освобождают
интервал. При попытке вернуть такую бронь в `new` или `confirmed` пересечение
проверяется повторно.

Публичная доступность содержит только время и не раскрывает данные гостей.
Сайт и бот показывают занятые старты недоступными, но окончательная проверка всё
равно выполняется при создании заявки. Бронь на 13:00 занимает 13:00–15:00 и
делает недоступными старты 12:00–14:30; следующий возможный старт — 15:00.

В разделе «Брони» панели владельца можно подтвердить или завершить заявку, а
отмена вынесена в отдельную кнопку с подтверждением. После отмены время сразу
освобождается; карточка сохраняет дату и источник отмены. Восстановление
отменённой брони повторно проверяет конфликт времени. Здесь же можно вручную
закрыть произвольное получасовое окно — например, для частного события.
Закрытое время учитывается и сайтом, и ботом.

Бот создаёт бронь с заголовком `X-Booking-Bot-Secret`. Значение
`BOOKING_BOT_SECRET` должно быть случайным (рекомендуется `openssl rand -hex
32`) и одинаковым в `/etc/chainya-bot.env` и окружении `chainya-shop`. Без
секрета интеграция закрывается безопасно и бронь не считается принятой.

Сырые события статистики хранятся не более 360 дней.

Если заданы `BOT_TOKEN` и `OWNER_CHAT_ID`, после первого перехода заказа в
`paid` backend отправляет владельцам уведомление через Telegram. Повторный вызов
эндпоинта оплаты не дублирует сообщение.

## Saby Retail

Клиент `backend/saby.py` поддерживает сервисную авторизацию, точки продаж,
прайс-листы, каталог с ценами и остатками, компании, склады и создание заказа на
доставку. В коде настроено 29 стабильных соответствий `id` + `externalId`.
Позиция «Травяной сбор „Тонизирующий“» не связана с витриной по указанию
владельца; это не удаляет её из кассы.

Отдельный `SABY_CATALOG_SHADOW_MODE=on` запускает только чтение каталога при
старте сервиса и затем каждые 300 секунд. Результат сохраняется в SQLite (не
более 50 запусков) и показывается в блоке «Теневая сверка каталога» панели
владельца. Для весового товара сайт хранит цену за 10 г. Проверка читает как
базовый каталог Saby, так и выбранный прайс-лист: если цена и остаток вместе
подтверждают продажную порцию 10 г, она безопасно нормализуется; в остальных
случаях цена сравнивается с базовой единицей в 1 г. Для штучного товара цена
сравнивается один к одному. Наличие требует минимум 10 г или одну штуку.
Неизвестные единицы и остатки не угадываются, а помечаются как несравнимые.

Боевой checkout при маршруте `fiscal_sale` дополнительно требует
`SABY_STOCK_GUARD_MODE=auto`. Перед созданием платежа backend читает базовый
физический остаток Saby, агрегирует одинаковые позиции корзины и атомарно
создаёт локальный резерв на 15 минут. Платёж Т-Банка остаётся одностадийным:
после оплаты деньги списываются сразу. Срок платёжной ссылки совпадает со сроком
резерва. Истёкшие и отменённые резервы освобождаются. Оплаченный резерв
перестаёт вычитаться дополнительно, когда соответствующее уменьшение уже видно
в Saby. Редкая одновременная офлайн-продажа после проверки остатка разбирается
как обычный возврат, а не через автоматический банковский холд.

Теневая сверка не вызывает API записи и никогда не обновляет каталог. Она не
зависит от `SABY_ORDER_SYNC_MODE` и сама по себе не разрешает передачу заказов.

Read-only проверка 10.08.2026 уточнила прежнее предупреждение по двум товарам.
Базовый каталог и кассовый интерфейс Saby показывают 17,5 ₽ и 16,5 ₽ за 1 г,
а прайс-лист «Сайт chainya.ru» возвращает те же товары как порции 10 г по
175 ₽ и 165 ₽ и одновременно делит остаток на 10. Владелец подтвердил эти
цены. Это не расхождение: теневая сверка определяет подтверждённую фасовку по
обоим каталогам и показывает её только как информационное пояснение. Никаких
цен или карточек в Saby проверка не изменяет.

В production `SABY_ORDER_SYNC_MODE=auto` включён с обязательной отсечкой
`SABY_ORDER_SYNC_STARTED_AT`; исторические и тестовые оплаты до неё не
передаются. Последние контрольные оплаты получили состояние `ambiguous`:
Saby вернул HTTP 500 без безопасного диагностического ответа, а фискальный признак не
подтверждён. Такие операции не повторяются автоматически и требуют ручной сверки
по времени и сумме.

Read-only проверка админ-панели отдельно показывает готовность
Retail-каталога и `fiscal_probe`: видит ли сервисное приложение компанию.
Список ККТ относится к отдельному OFD API с login/password-сессией (`sid`),
поэтому сервисный OAuth Retail не используется для ложной проверки кассы.
Состояние `separate_auth_required` означает именно это ограничение read-only
диагностики, а не отсутствие прав на регистрацию продажи. Ни один GET-запрос
не делает пробную продажу и не подтверждает фактическую регистрацию чека.

### Как оплаченная покупка попадает в Saby

Заказ, фискальный чек и складское списание — разные операции. Переменная
`SABY_PURCHASE_ROUTE` делает выбранный маршрут явным:

- `delivery` — действующий адаптер создаёт заказ через Saby Retail Delivery;
- `fiscal_sale` — API Saby OFD создаёт продажу вместе с фискальным чеком;
- `external_ofd` — Saby импортирует уже сформированные чеки поддерживаемого
  внешнего ОФД; это настраивается в кабинете Saby.

По умолчанию используется `delivery`, чтобы не менять существующее поведение.
`fiscal_sale` реализован как отдельный fail-closed маршрут: он доступен только
при сервисной авторизации Saby, настроенном `SABY_POINT_ID`, регистрационном
номере ККТ и отключённом чеке
Т-Банка. Каждая продажа и каждый полный возврат имеют собственный стабильный
`externalId`; потерянный ответ Saby переводится в ручную сверку и никогда не
повторяется вслепую. После принятия продажи backend отдельно опрашивает чек и
считает его зарегистрированным только после появления фискального признака;
пустые реквизиты означают ожидание ОФД, а не повторную продажу. Фискальный
признак принимается только из строки ответа с тем же `payId`, который вернула
регистрация: подписанный чужой или лишний чек не может закрыть заказ как
успешный. Уже полученный `payId` остаётся привязан к фискальному маршруту даже
после изменения `SABY_PURCHASE_ROUTE`: очередь продолжает только GET-проверку и
не может перенаправить принятую операцию в Delivery. Прерванный до сохранения
ответа фискальный POST аналогично становится `ambiguous` независимо от текущего
маршрута. По официальной схеме «полного расчёта» склад Saby списывает товар
после закрытия кассовой смены, а не сразу после callback банка. До отражения
этого списания checkout продолжает учитывать локальную оплаченную резервацию,
чтобы свежий, но ещё не уменьшившийся остаток Saby не позволил продать те же
единицы повторно. `fiscal_sale` нельзя включать одновременно с
`TBANK_RECEIPT_ENABLED=1`: иначе одна оплата может получить два фискальных
чека. `external_ofd` и неизвестное значение остаются заблокированными.

Сервисное приложение авторизуется через `online.sbis.ru/oauth/service/` и
передаёт полученный `token` в `X-SBISAccessToken`. Это не сессионная
login/password-схема OFD с `sid`; смешивать эти два контракта нельзя. Ответы
фискальных методов дополнительно нормализуются: если полезный JSON вернулся
строкой в поле `Result`, backend безопасно распакует его перед чтением
`payId` или фискального признака. Некорректный `Result` считается
неопределённым результатом и не вызывает слепой повтор `POST`. После `HTTP 401`
токен автоматически обновляется только для безопасных `GET`: любой уже
отправленный write-запрос остаётся неопределённым и также не повторяется.
Значения `none`, `null`, `None` и пустая строка в поле фискального признака
означают, что чек ещё не сформирован, поэтому сохраняется `pending_ofd`.

Весовые позиции передаются в килограммах с точностью до `0.001`, чтобы
фасовки 10/25/50/100 г не искажались при сериализации. Перед отправкой builder
повторно проверяет уже строковые значения цены, количества и суммы; любое
расхождение даже в копейку блокирует регистрацию чека локально.

Если Saby отвечает HTTP-ошибкой с безопасным текстовым пояснением в JSON,
backend сохраняет только одну из заранее заданных операторских категорий
вместо произвольного текста поставщика. Поэтому имена покупателей, реквизиты
ККТ, токены, телефоны, email, URL и идентификаторы из ответа в панель и логи не
переносятся. Каждый запрос получает локальный correlation ID, включая случай
HTTP 200 с некорректным строковым `Result`. Глобальный лимитер разрешает не
более двух одновременных запросов к Saby. Для состояния `ambiguous` повтор
полностью запрещён в панели и фоновой очереди. После сверки Saby отдельное
решение по такой операции принимают владелец и технический ответственный; тот
же фискальный `POST` штатная система не выполняет.

Непосредственно перед фискальным `POST` backend заново читает базовый каталог
Saby и требует точного совпадения названия каждой позиции по сохранённому
`externalId`. Несовпадение или недоступность read-only проверки переводят
операцию в безопасное повторяемое состояние до отправки, поэтому дублирующая
номенклатура не создаётся. Для платной CDEK-доставки дополнительно требуется
ровно одна существующая позиция Saby с точным названием «Доставка».
`pending_ofd` проверяется только чтением; если
фискальные признаки не появились за 24 часа, опрос останавливается с видимым
блокером и без повторной продажи.

Локальный `blocked` фискального маршрута, возникший до сборки payload и до
любого provider POST, автоматически возвращается в очередь после исправления
его локальной конфигурации. Это разрешено только при отсутствии `payId` и хеша
payload. Delivery зависит от внешней проверки продукта и автоматически так не
возобновляется. `ambiguous`, известный чек и любой блокер после принятой
операции этой логикой никогда не повторяются.

Если Saby уже вернул `payId`, владелец может нажать в заказе «Проверить чек
Saby». Эта операция вызывает только `/retail/pay/list` для известного чека и
никогда не повторяет `/retail/sale/create`. Настоящий фискальный признак
переводит чек в `registered`; документированный `fiscalSign: "none"` оставляет
его в `pending_ofd`. Просроченный `blocked` не возвращается в очередь одним
ручным нажатием. Если `payId` нет, кнопка не показывается: нужна ручная сверка
по времени и сумме, а повтор продажи остаётся запрещён.

Если Т-Банк подтвердил полный возврат, пока принятая продажа ещё находится в
`pending_ofd`, очередь продолжает только `GET` уже известного `payId`. После
появления фискального признака исходной продажи автоматически ставится
отдельный возвратный чек; повторный `POST` продажи запрещён. Если продажа была
безопасно отклонена до внешнего `POST`, возврат не создаёт ни продажу, ни
возвратный чек Saby. Неопределённая продажа по-прежнему требует ручной сверки.

В контракте `/retail/sale/create` Saby описывает `companyID` как идентификатор
точки продаж и рекомендует получить точное значение для организации у
поддержки. Chainya не копирует его незаметно: для маршрута записи требуется
явно заданный `SABY_OFD_COMPANY_ID`. Консервативная политика проекта требует,
чтобы он совпадал с выбранным `SABY_POINT_ID`; иной подтверждённый Saby контракт
потребует отдельного изменения кода. Отсутствие любого значения или их
расхождение оставляет маршрут и checkout закрытыми. В secret-free статусе это видно как
`point_binding_confirmed`, без публикации самих идентификаторов.

Payload регистрации следует фактическому JSON-примеру Saby: неиспользуемые
суммы оплаты и НДС, а также отсутствующие email/ИНН передаются как `null`, а
значение дополнительного реквизита — в поле `propVal`. В таблице параметров
это имя сейчас сокращено до `propVa`, но в опубликованном примере wire-запроса
используется полное `propVal`.

По умолчанию любая неоднозначная фискальная операция Saby закрывает checkout.
Для временной работы с ручным пробитием владелец может явно установить
`SABY_AMBIGUOUS_CHECKOUT_POLICY=manual`: новые оплаты продолжат приниматься, а
неоднозначные операции останутся видимыми в админ-панели и никогда не будут
повторены автоматически. Неизвестное значение настройки закрывает checkout.

`SABY_OFD_PAY_METHOD` намеренно не имеет значения по умолчанию. Код пока
реализует только значение `4`: после оплаты Saby создаёт продажу и чек «полный
расчёт», без второго чека при выдаче. Одностадийное списание денег Т-Банком не
доказывает правильность этой фискальной схемы для отложенного самовывоза или
CDEK. Поэтому `4` задаётся только после бизнес-подтверждения конкретного
порядка передачи товара. Продажа и полный возврат имеют разные стабильные
`externalId`, а потерянный ответ не повторяется автоматически. Временно
разрешённая владельцем ручная обработка инцидентов задаётся отдельно через
`SABY_AMBIGUOUS_CHECKOUT_POLICY=manual`; она не делает неопределённую операцию
успешной и не разрешает её автоматический повтор.

Переключение выполняется только по `ops/SABY_FISCAL_CUTOVER.md`. Оно требует
контрольного чека и подтверждения его появления в ККТ/ОФД до открытия
публичной оплаты.

Скопируйте значения из `backend/saby.env.example` в защищённый серверный файл
`/etc/chainya-shop-saby.env`. После добавления трёх ключей в админ-панели станет
доступна кнопка проверки подключения. После выбора точки и прайс-листа добавьте
`SABY_POINT_ID` и `SABY_PRICE_LIST_ID` и перезапустите `chainya-shop`.

## Т-Банк и CDEK

Шаблон новых параметров — `backend/integrations.env.example`, серверный файл —
`/etc/chainya-shop-integrations.env` с правами `0600`. Режимы по умолчанию:
`TBANK_CHECKOUT_MODE=off`, `CDEK_INTEGRATION_MODE=off` и
`SABY_ORDER_SYNC_MODE=off`. Безопасный `SABY_CATALOG_SHADOW_MODE=on` управляет
только периодическим чтением и не снимает ни один предохранитель записи.

При первом включении `SABY_ORDER_SYNC_MODE=auto` обязательно задайте
`SABY_ORDER_SYNC_STARTED_AT` текущим ISO-8601 временем с часовым поясом. Заказы,
оплаченные раньше этой отметки, автоматически в Saby не передаются: это защищает
от отправки накопленных тестовых и исторических заказов.

Для локальной проверки поддерживается отдельный DEMO-терминал:
`TBANK_CHECKOUT_MODE=demo` разрешён только при `CHAINYA_TEST_MODE=1` и только
для ключа с суффиксом `DEMO`; Saby и CDEK при этом остаются заблокированы.
Боевой терминал работает только при `CHAINYA_TEST_MODE=0`,
`TBANK_CHECKOUT_MODE=auto` и ровно одном готовом источнике чека: настроенном
онлайн-чеке Т-Банка либо готовом маршруте `fiscal_sale` Saby. Checkout
сохраняет `PaymentId`/`PaymentURL`, дедуплицирует повторный запрос по
`Idempotency-Key`, а оплатой считает только подписанный callback `CONFIRMED` на
`/api/payments/tbank/notification`. Страницы `/payment/success` и
`/payment/fail` сами статус не меняют.

Полный возврат тестового/боевого платежа доступен владельцу через
`POST /api/admin/orders/{id}/tbank/refund` и кнопку в панели. Повторный запрос
не создаёт второй возврат.

Если webhook банка потерялся, кнопка «Проверить оплату» сначала только читает
`GetState`. При безопасно доказуемом расхождении она превращается в
«Восстановить статус»: второе явное нажатие обновляет локальный заказ, но не
повторяет денежную операцию. `CONFIRMED` никогда не отменяет уже начатый или
неопределённый возврат; такой возврат завершается локально только после
`REFUNDED` от Т-Банка.

Для маршрута Т-Банка код не разрешает боевой режим `auto` без полностью
настроенного онлайн-чека:
после подтверждения СНО, ставок НДС и версии ФФД задаются `TBANK_RECEIPT_*` и
`TBANK_RECEIPT_ENABLED=1`; серверный чек передаётся в `Init`. При полном
`Cancel` структура чека по текущему контракту Т-Банка не передаётся: банк
формирует полный возвратный чек автоматически; для частичного возврата нужен
чек только с возвращаемыми позициями. 04.08.2026 представитель проекта
подтвердил УСН «Доходы», отсутствие НДС для чая и доставки и ФФД 1.05;
production-конфигурация совпадает с этими значениями. До первого реального
платежа всё равно нужен контролируемый тест по `ops/PRELAUNCH_CHECKLIST.md`;
менять защищённый env по догадке нельзя.

Ключи CDEK API хранятся только в `/etc/chainya-shop-integrations.env`.
Публичная корзина ищет города и ПВЗ через сервер, а цену считает по тарифам
«Посылка склад-склад» (136) и «Посылка склад-дверь» (137). Вес определяется по
фасовкам в корзине плюс `CDEK_PACKAGING_WEIGHT_G`; габариты задаются
`CDEK_PACKAGE_*`. При создании заказа сервер повторяет расчёт и проверяет, что
ПВЗ действующий и относится к выбранному городу.

Расчёт и справочники не создают внешних сущностей. Регистрация оплаченного
отправления доступна отдельной кнопкой владельца только после установки
`CDEK_SHIPMENT_POINT`, отключения общего тестового режима и явного режима
`CDEK_INTEGRATION_MODE=manual`. В `off`/`shadow` создание отправлений
заблокировано независимо от наличия ключей.
